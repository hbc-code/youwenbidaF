//?问题管理页面修改问题的信息保存
//问题id
export const questionId = (pre,action) => {
    const {type,value} = action;
        return value;
    }
//问题标题
export const questionChangeTitle = (pre,action) => {
    const {type,value} = action;
        return value;
    }
//问题描述
export const questionChangeDescribe = (pre,action) => {
    const {type,value} = action;
    return value;
}
//问题图片，没有用到
export const questionChangeImg = (pre,action) => {
    const {type,value} = action;
    return value;
}
//提问者学号
export const stuId = (pre,action) => {
    const {type,value} = action;
    return value;
}

//举报id
export const reportId = (pre,action) => {
    const {type,value} = action;
    return value;
}

//反馈id
export const feedbackId = (pre,action) => {
    const {type,value} = action;
    return value;
}
//学科id
export const subjectId = (pre,action) => {
    const {type,value} = action;
    return value;
}
//日志id
export const logId = (pre,action) => {
    const {type,value} = action;
    return value;
}

//学科名称
export const subjectName = (pre,action) => {
    const {type,value} = action;
    return value;
}
//学科所属学院
export const college = (pre,action) => {
    const {type,value} = action;
    return value;
}
//学科简介
export const subjectInfo = (pre,action) => {
    const {type,value} = action;
    return value;
}
//学科备注
export const subjectNote = (pre,action) => {
    const {type,value} = action;
    return value;
}
//学科icon
export const subjectIcon= (pre,action) => {
    const {type,value} = action;
    return value;
}

